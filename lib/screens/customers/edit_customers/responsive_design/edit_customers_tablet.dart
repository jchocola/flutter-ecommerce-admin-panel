import 'package:flutter/material.dart';

class EditCustomersTablet extends StatelessWidget {
  const EditCustomersTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Edit Customers Tablet')
        ],
      ),
    );
  }
}