import 'package:flutter/material.dart';

class EditCustomersMobile extends StatelessWidget {
  const EditCustomersMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Edit Customers Mobile')
        ],
      ),
    );
  }
}