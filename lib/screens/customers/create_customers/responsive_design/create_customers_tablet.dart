import 'package:flutter/material.dart';

class CreateCustomersTablet extends StatelessWidget {
  const CreateCustomersTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Create Customers Tablet')
        ],
      ),
    );
  }
}