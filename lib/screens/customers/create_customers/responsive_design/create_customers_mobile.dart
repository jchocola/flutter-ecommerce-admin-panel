import 'package:flutter/material.dart';

class CreateCustomersMobile extends StatelessWidget {
  const CreateCustomersMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Create Customers Mobile')
        ],
      ),
    );
  }
}