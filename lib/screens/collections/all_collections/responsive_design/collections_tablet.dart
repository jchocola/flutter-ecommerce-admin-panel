import 'package:flutter/material.dart';

class CollectionsTablet extends StatelessWidget {
  const CollectionsTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}