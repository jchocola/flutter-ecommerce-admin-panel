import 'package:flutter/material.dart';

class CollectionsDesktop extends StatelessWidget {
  const CollectionsDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}