import 'package:flutter/material.dart';

class CollectionsMobile extends StatelessWidget {
  const CollectionsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
