import 'package:flutter/material.dart';

class ReviewsDesktop extends StatelessWidget {
  const ReviewsDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}