import 'package:flutter/material.dart';


class CreateOrdersDesktop extends StatelessWidget {
  const CreateOrdersDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Desktop')
        ],
      ),
    );
  }
}