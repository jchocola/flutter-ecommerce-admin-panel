import 'package:flutter/material.dart';


class CreateOrdersTablet extends StatelessWidget {
  const CreateOrdersTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Tablet')
        ],
      ),
    );
  }
}